# AgriLink API

Fresh Express API for farmers, buyers, and administrators. It uses an in-memory store for the demo so it runs without a database; replace the arrays with MongoDB or PostgreSQL when deploying. Set `JWT_SECRET` and `PORT` from `.env`.

Run `npm install`, then `npm start`.
